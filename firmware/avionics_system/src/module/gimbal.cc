#include "gimbal.hh" 

// Servo x_servo;
// Servo z_servo;

double pos = 0;

int gimbal_ns::init_gimbal() {

  // x_servo.attach(X_PIN);
  // z_servo.attach(Z_PIN);


  // for (pos = 85.0; pos <= 95.0; pos += 0.05){
  //   x_servo.write(pos);
  //   z_servo.write(pos);

  //   delay(500);
  // }

  // for (pos = 95.0; pos <= 85.0; pos -= 0.05){
  //   x_servo.write(pos);
  //   z_servo.write(pos);

  //   delay(500);
  // }

  // x_servo.write(90);
  // z_servo.write(90);


  return 0; //Test return for valid init
}