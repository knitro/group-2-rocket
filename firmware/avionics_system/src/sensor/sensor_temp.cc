#include "sensor_temp.hh" 

// using namespace sensor_temp_ns;

/*Functions*/
int sensor_temp_ns::init_temp() {
  //Perform some initialisation of the sensor
  
  return 0; //Test return for valid init
}

int sensor_temp_ns::record_data_temp() {
  //Record the data, and add it to the buffer

  return 0; //Currently returning 0 regardless of sensor being available or not
}


int sensor_temp_ns::get_data_temp(sensor_temp_ns::temp_data_t* return_data) {

  return 0; //Currently returning 0 regardless of sensor being available or not
}